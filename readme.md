## Mulitmodal poc

``` bash
cd backend
python -m venv venv
pip install -r requirements.txt
```

``` bash
cd backend
venv/Scripts/activate
uvicorn main:app --reload
```

for frontend..

```
cd frontend
npm install
npm run dev
```